MEMORY {
    ROM: start = $F000, size = $1000, file = %O, fill = yes, fillval = $FF;
    RAM: start = $0300, size = $0100, file = "";
}
SEGMENTS {
    CODE: load = ROM, type = ro;
    BSS: load = RAM, type = bss;
    VECTORS: load = ROM, type = ro, start = $FFFA;
}
